<!-- Stats -->
<div class="bg-white rounded-lg shadow-sm p-6">
    <h2 class="text-lg font-semibold mb-4">Statistics</h2>
    <div class="space-y-4">
    <div>
        <div class="text-sm text-gray-500">Total Events</div>
        <div class="text-2xl font-semibold"><%= @account.total_events_count %></div>
    </div>
    <div>
        <div class="text-sm text-gray-500">Upcoming Events</div>
        <div class="text-2xl font-semibold"><%= @account.upcoming_events_count %></div>
    </div>
    <div>
        <div class="text-sm text-gray-500">Past Events</div>
        <div class="text-2xl font-semibold"><%= @account.past_events_count %></div>
    </div>
    </div>
</div>